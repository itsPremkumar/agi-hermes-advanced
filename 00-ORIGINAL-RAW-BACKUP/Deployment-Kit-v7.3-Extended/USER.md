# User Profile

<!--
  Preferences, projects, and working style Hermes learns about you over
  time. Stays under the user_char_limit set in config.yaml (~1375 chars /
  ~500 tokens by default). You can hand-edit this directly if you'd rather
  state a preference outright than wait for the agent to infer it —
  hand-authored and agent-inferred entries can coexist here.

  Good candidates: how you like things communicated (terse vs. detailed),
  recurring projects, tools/environments you use, standing constraints.
  Not good candidates: anything sensitive — this file is plain text in
  every session's context, not a secrets store. Secrets belong in .env.
-->

- Building a Hermes Agent deployment kit: SKILL.md (executive/planning
  protocol) + SOUL.md (identity/values/limits) + config.yaml/.env/MEMORY.md/
  USER.md (runtime scaffold).
- Wants comprehensive, substantive output over padded or generic content —
  calls out filler directly; prefers a shorter honest answer to a longer
  fabricated one.
- Values technical accuracy and real citations over polished-sounding but
  unverified claims.
